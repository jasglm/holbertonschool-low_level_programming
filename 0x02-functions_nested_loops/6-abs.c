#include "main.h"

/**
 * _abs - prints absolute value of int
 * @n: character being exmined
 * Return: value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
